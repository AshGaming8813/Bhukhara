import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.Bhukhara.aab',
  appName: 'Bhukhara',
  webDir: 'dist',
  server: {
    androidScheme: 'https',
  },
};

export default config;
